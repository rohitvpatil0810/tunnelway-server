# Tunnelway server settings
# Domain used to build the public tunnel URL shown to agents: <slug>.<main-domain>
TUNNELWAY_MAIN_DOMAIN=localtest.me

# HTTP listen address for the server. Use 0.0.0.0 when running in Docker.
TUNNELWAY_LISTEN_ADDR=0.0.0.0:6000
